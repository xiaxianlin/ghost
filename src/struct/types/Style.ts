export enum TextAlign {
    LEFT = 'left',
    CENTER = 'center',
    RIGHT = 'right',
}

export enum TextVerticalAlign {
    BASELINE = 'left',
    TOP = 'center',
    MIDDLE = 'right',
    BOTTOM = 'bottom',
    SUB = 'sub',
    TEXTTOP = 'text-top',
}
